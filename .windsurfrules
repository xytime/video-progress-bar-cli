# Video-Processing Project - Global Instructions & AI Cognitive Defense

## 1. 认知防范体系 (Cognitive Defense)
- 在对本项目核心模块 (如 `src/video_processing/processors/` 下的代码) 进行重大修改前，必须先读取 `docs/experience_log/critical_lessons.md`。
- 不可忽略视频处理场景下特有的声学、格式和 UI 对齐限制。

## 2. 视频与字幕处理规范
- **ASS 字幕换行铁律**: pysubs2 不支持 Python 的 `\n` 换行，必须在事件生成前执行 `text.replace('\n', '\\N')`。
- **色彩空间约束**: 高级色彩配置（`pysubs2.Color`）严格依赖 (R, G, B) 整数。
- **双语排版动态 Margin**: 必须计算底层字幕的真实换行数，动态推高上层字幕的 `marginv`，严禁固定偏移。
- **Whisper 音频格式**: 抽取音频送入 Whisper 必须统一重采样为 `16000Hz 单声道` (`-ar 16000 -ac 1`)。

## 3. 工具使用协议
- 下载外部视频时，统一下载至临时目录或 `output/`。
- 加工字幕优先使用 `auto-caption` 命令行工具。
- 加工完毕后输出发布文案（包含标题、短标题、简介）。
