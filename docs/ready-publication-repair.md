# 成片就绪即发布修复

2026-09-23，Codex。用户已确认独立发布调度、待发布环节及缓存恢复规格。

依赖方向：`scripts/run_ready_publications.py`、`web/app.py` → `PipelineManager` → `PipelineDB`、`core/task_lease.py`、`utils/file_utils.py` → `settings`。锁模块只依赖标准库，不反向导入调度器。

加工与发布使用独立进程；加工锁不覆盖成片发布。单视频锁覆盖本条产物读写，发布领取使用数据库原子状态迁移；同账号上传仍由浏览器会话锁保护。成片就绪由既有持久化 preparation_ready 加就绪时间表达，对外展示为待发布，不伪造平台成功。独立执行者每 15 秒巡检，不读取发布时段或重负载避让开关。提交前重新验证成片、文案、封面和内容审查；已有提交证据先于产物检查阻止重发。

下载状态只用于真实下载；未完成任务依赖的原片受保留保护；下载器降级先验证已完成格式流，保留可续传片段，隔离已确认损坏的文件。

验证须覆盖真实跨进程锁、并发领取、缓存失败重试、孤儿恢复、页面归属及原因。测试通过、运行采用、平台受理、公开可见分别报告。

## 2026-09-23 实施记录

已完成与生产主干 `be2e469` 的整合：保留下载策略、逐条领取、异步审核物料及 PID 归属改进，共用清理入口改为保留有效缓存和续传文件。无关的选题线索、架构扫描及命令行改动保持原样。

整合验收：`2080 passed, 15 warnings, 30 subtests passed in 100.91s`。由生产虚拟环境执行 `scripts/run_isolated_tests.py --browser -- -q tests/unit`，系统隔离探针及真实 Chromium 检查通过。下载回退测试使用真实音视频验证，完整格式流、`.part` 与续传元数据均保留。

采用流程：核对生产文件哈希、取得加工与巡航内核锁并确认没有在途任务，仅重启面板；将独立发布守护进程的版本、心跳及页面回读保存到任务附件 `ready-publication-verification.md`。代码提交或心跳正常均不代表平台已经公开发布。

事故两条任务的 09:54 只读回查：`lXWVGT7aZVo` 为 `SUBMITTED_BOUND`，已绑定同次受理的原生 ID，但无公开可见证明；`pnOa9q2beec` 在旧执行者 09:42 的上传子进程失败，数据库错误为空，只有原创声明决策文件，没有受理回执。日志明确显示其原片、字幕、成片及封面全部复用，未重新下载。具体上传失败原因没有被旧日志保存，不据此推断平台拒绝，也不自动补发。
