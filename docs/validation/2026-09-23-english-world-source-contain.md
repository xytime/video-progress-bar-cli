# 英语世界原视频窗口完整缩放修复

用户反馈已受理的 `0nRIV_FXMnA` 学习卡左上人物被裁掉半个头，明确要求恢复完整缩放。

实际原片为 1080×1920、SAR 1:1 的竖视频，窗口为 646×363。旧渲染器使用 `scale ... increase,crop`，放大后只保留约 31.6% 的原画面高度。该裁切在此学习卡渲染器初版 `9617f77` 已存在；不是原始素材缺头。此前内容安全复核及人工画面核验未识别此构图缺陷。

修复使用显示比例 DAR 计算可完全容纳的尺寸，统一方形像素，居中填充原模板底色；不裁切、不拉伸。新 manifest 标记 `source_video_fit=contain` 和原窗口坐标。正文、词轴、来源区间及原语言审校输入不变。

43 项隔离测试通过，证据目录 `/private/tmp/video-pytest-efyhp4mb`。新增真实 FFmpeg 测试以不同颜色标注原画面四角，验证五种比例（竖、横、超宽、方形、非方形像素）输出四角全部保留、显示比例正确且居中，同时回归模板与音频边界。

同一已审校来源重新渲染 43.14 秒修正版：`output/english_world_contain_fix/english_world_contain.mp4`，位于本任务隔离工作树。SHA256：`c4206782253b1a2d2106312f4c005c882f5b7a781235cb332d83153ecdc6a89b`。新成片本地 Whisper 音频 QA、冻结版式核验及独立 AGY 视觉内容安全全部 PASS；开头、20 秒中段、滚动后及末句抽帧检查原画面完整。

最终文本、频道规则及视觉证据绑定安全门 PASS（`qa/safety_gate.json`）。首次绑定因时间线与新联系表跨目录被拒绝；将时间线、展示计划、来源证据逐字节复制到修正版目录并核验 SHA256 相同后重检通过，没有放宽门禁。

修正版为独立本地文件。已受理旧片、原审核回执及平台 ID 保持原样；本次修复没有撤稿、替换或重复投稿。
