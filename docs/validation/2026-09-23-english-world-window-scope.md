# 英语世界固定自然句窗口修复

2026-09-23，用户批准内容安全绝不让步，其他质量要求可以适当宽松。本次只调整 ASR 时间检查的作用范围，保留所有既有内容安全、语言、音频及视觉检查。

问题：程序先验证完整 120 秒转写，之后才选实际自然句片段。具名来源 `0nRIV_FXMnA` 的 medium 转写在 64.66 秒有孤立零宽词，但固定的首个自然句窗口为 0–43.14 秒。

改动：先固定窗口，再严格验证前缀和后文边界。保留完整原始转写与全文上下文；只允许现有、由真实锚点支持的时间修复。同源窗口全局冻结，重复运行不改选句末。历史 medium FAIL 保持原样，范围复算另留回执；不额外转写，不重置语言审校预算。派生字幕按原始词序、文字和已验证时间逐项核验。

验证：隔离测试 56 项通过，证据目录 `/private/tmp/video-pytest-1s1u_of_`。覆盖片段外零宽词、片段内零宽词、跨界词、倒退/非有限时间、失败后禁止跳到下一句、跨目录复用、旧 FAIL 字节不变、派生字幕锚点篡改拒绝，以及内容安全仍阻断。

真实回放在临时目录完成：窗口 43.14 秒，124 个词，1 处已有同锚点组修复；未运行 ASR，原生产失败回执哈希不变。此结果只证明来源预检恢复，不能代替完整制作验收、平台受理或公开发布证据。

实际 CLI 运行暴露包导入路径问题，已改为调用 `bootstrap-bind` 文件入口，并增加真实子进程入口回归；56 项测试再次通过，证据目录 `/private/tmp/video-pytest-m7swegd8`。修复由生产 main 采用并推送至 `98648f5`，采用时验证 28 个无关脏文件保持不变。

来源与候选文本安全预检恢复后，AGY 初稿未返回；依既有 Codex 起草协议，在同一工作目录完成初稿、离线词典与展示准备。独立审校实际遇到 `429 / RESOURCE_EXHAUSTED / Individual quota reached`，服务提示恢复时间约为 14:31:10 +08:00（14:16:05 日志提示 15m5s）。这不是内容审校 FAIL。

增加显式额度恢复入口，仅允许同一输入在确认冷却结束后动用原三次总预算中的第三次；不删除或重置账本，不允许覆盖任何已完成内容结论。140 项隔离测试通过，证据目录 `/private/tmp/video-pytest-gybz62ni`；覆盖所有次数/状态拒绝、未来或非法恢复时间、输入改变、已完成内容失败以及最终一次仍失败的终止行为。实际补发结果另以审核与平台回执为准。
