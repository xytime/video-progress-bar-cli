# 2026-09-25 英语世界停更调查复核与修复边界

> 核查时间：2026-09-25，北京时间。当前代码在生产 `main` 工作区；文件改动会被后续调度直接读取。本文不代表新作品已生成、已提交或已公开。

# Modification History

| Version | Date | Author | Description |
| --- | --- | --- | --- |
| 1.1.0 | 2026-09-25 | Codex | 记录已确认规格的实现、隔离验收及三条历史记录的只读复核结果。 |
| 1.0.1 | 2026-09-25 | Codex | 补记 14:00 生产槽位首次真实 AGY 成功与平台身份未绑定的剩余风险。 |
| 1.0.0 | 2026-09-25 | Codex | 复核外部调查报告，记录 AGY Schema 修复、验证结果和待确认的对账与告警规格。 |

## 已核实的生产阻断

- `english_world_programmatic_daily.py` 在初稿和修订 Schema 的整数 `word_index` 上添加整数 `enum`。调查报告保存的 AGY 底层错误为 `INVALID_ARGUMENT (code 400)`，指向 `learning_points.word_index.enum`。本地代码与该故障机制吻合；本轮没有重新向外部 AGY 发送真实时间轴。
- 现已去除发给 AGY 的整数枚举，保留索引上下界；无词典候选时仍将学习点数量上限设为 0。初稿与修订收到结果后，宿主代码会拒绝不在词典候选中的索引，包括已被独立审校否决发音的词。
- AGY 非零退出时仍不保存原始 stdout/stderr，以免回显字幕或凭据；错误现在保留安全分类及退出码，可进入运行日志和 `candidate_failure.json`。结构化参数的 `INVALID_ARGUMENT` 归类为 `invalid schema`。
- 指定的隔离测试 42 项通过；`git diff --check` 通过。以 2026-09-25 10:02 的原时间轴只读构建 Schema 得到 64 个词、53 个词典候选，`word_index` 中无 `enum`。手动外发实测因自动审批拒绝而未执行；随后 14:00 的正常生产调度提供了真实 AGY 成功证据，见下节。

### 14:00 生产槽位后续回读

- 2026-09-25 14:00 启动的真实日更在首次尝试中完成 `agy_draft`、语言审校、渲染、音频 QA 和安全检查；协调器 14:14:51 以退出码 0 结束。这证明当前这条来源的生成硬阻断已被跨过；不能据一次成功保证未来每次运行。
- 本地交付回执为 `QA_PASSED`、`ACCEPTED`、`UNDER_REVIEW`。对应 review ID `00c4ee42bebe4e90a4cf858c679142bd` 的投稿尝试在 14:14:48 结束，但 `platform_post_id` 仍为空。投稿回执理由为 `No unique before/after platform-ID delta with exact title`。用户报告已在平台看到新视频；本机账本尚无法把该作品与唯一平台原生 ID 绑定，因此也不能由自动对账证明公开状态。
- 这是与 AGY Schema 不同的剩余卡点。下一次日更可能继续生成与提交，但无法承诺平台身份绑定、平台审核和公开回读均不会延迟。

## 调查报告中须保留的不确定性

- 2026-09-22 的 `9ef5a56d...` 和 2026-09-23 的 `9e7af918...` 均有绑定原生 Post ID；只读回查得到数值状态 `1`，记录为 `UNCERTAIN`，连续两次后达到当前回查阈值。作者评论管理页截图证明当时能找到并评论相应作品，但不能确立状态码 `1` 对所有作品、时点的公开语义。
- 2026-09-24 的 `0680a0f4...` 已有提交结束时间，但没有绑定原生 Post ID；不能按标题猜测身份、直接归档或重传。
- “9 月 21 日起没有新作品公开”只能表述为“账本没有新的平台公开确认”；已有作者评论证据表明后来作品至少在作者侧评论管理页可见。真实公众可见性需独立回读。
- 抖音 UI 熔断与 AGY 生成错误是不同故障。修复 AGY Schema 不会自动解除抖音熔断，也不会修复历史对账状态。

## 已确认的修复规格与验收

1. **历史作品精确复核**：仅针对上述两条已绑定作品与一条未绑定作品做只读平台回查。已绑定作品必须按原生 ID 命中，且有明确“已发布/公开”页面状态和留存凭据，才将账本提升为 `PUBLISHED`；数值 `1`、播放量、作者评论及标题相似均单独保留为辅助线索。未绑定作品先寻找同次提交的原生身份凭据，找不到则维持未确定。所有结果均禁止自动重传。
2. **回查耗尽后的可见状态**：达到两次不确定阈值时保留受理事实，发一条需人工核查的告警；人工发起的具名只读复核可越过自动阈值一次，结果仍按同一证据门禁归档。自动巡航不无限循环。
3. **业务零确认告警**：在至少有一个应执行日更窗口的前提下，连续 24 小时没有新增视频号公开确认时，使用现有 Telegram 通道最多每 24 小时发送一次 P1 状态提醒，列出最后确认时间、最近运行失败阶段、已受理但未确认的数量。提醒文案必须写“公开确认缺失”，不能断言真实未公开，也不触发补发、清锁或放松质检。

用户于 2026-09-25 确认上述方案。现已实现：具名审核编号的一次性只读回查可越过自动两次失败阈值；公开归档同时检查同一原生 ID 的结构化明确状态与页面截图；连续 24 小时无新增公开确认时，业务提醒先原子占位，每 24 小时最多尝试一次。提醒不触发补发。作品管理搜索可用标题缩小旧记录列表，但最终身份和状态判定仍只依据原生 ID 与显式状态。

隔离测试覆盖原生 ID 不一致、状态仅有未映射数值、缺失截图、无 ID 拒领、人工仅领取一次、自动阈值、提醒冷却，以及上传器退出码 0 时仍须匹配回读凭据。`test_publish_confirmation.py`、`test_publication_window_runner.py`、`test_english_world_jobs.py` 的隔离运行退出码为 0；运行器证据目录为 `/private/tmp/video-pytest-grjfqnrw`。这证明本地规则，不代表平台以后不会变更页面或审核速度。

### 历史作品只读回查结果

- `9ef5a56d...`：具名回查已使用一次，作品管理页可打开，但目标原生 ID 未在本次列表回读中找到；留存 `management_uncertain.png`。`platform_state` 继续为 `UNCERTAIN`，失败计数为 3，没有提升 `PUBLISHED`。
- `9e7af918...`：具名回查已使用一次，接口按原生 ID 命中记录，但只得到未映射的数值状态 `1`；标题筛选后的页面没有对应作品卡片的明确状态。留存 `management_readback.json` 与 `management_uncertain.png`。仍为 `UNCERTAIN`，失败计数为 3。
- `0680a0f4...`：同次投稿尝试和 `submission_receipt.json` 均没有原生 ID；提交后列表截图不能提供同次唯一身份绑定。未进行无 ID 的具名回查，状态保持未确定，也没有重传。

截至本次复核，平台公开账本最后确认时间仍为数据库 UTC `2026-09-20 15:29:02`，已受理但未确认共 4 条。`english_world.public_confirmation_gap` 提醒在 2026-09-25 06:32 UTC 取得 Telegram API `ACCEPTED` 回执；这不证明手机端展示或已读。9 月 25 日新视频用户报告已可见，但本机仍无唯一原生 ID 绑定；后续公开确认与平台时延不能保证为零。
